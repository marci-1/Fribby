<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.PasswordApp"
        tools:targetApi="31">
        <activity android:name=".features.ui.home.FindFriendsActivity" />
        <activity android:name=".features.ui.home.SetupActivity" />
        <activity android:name=".features.ui.home.HomeActivity" />
        <activity android:name=".features.ui.home.ProfileActivity" />
        <activity android:name=".features.ui.home.SettingsActivity" />
        <activity android:name=".features.ui.interest.InterestsActivity" />
        <activity android:name=".features.ui.home.ChatActivity" />
        <activity android:name=".features.ui.home.ViewInterestsActivity" />
        <activity
            android:name=".core.ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>